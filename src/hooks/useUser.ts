
// User hook
export function useUser() {
  // Implementation pending - will return { id, role, department, collegeId }
  return {
    id: null,
    role: null,
    department: null,
    collegeId: null
  };
}
